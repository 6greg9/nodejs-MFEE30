extends ../_layout/master 
block content
    h1 detail
    table 
        tr 
            th 日期
            th 收盤價
        each stock in stockData
            tr 
                td #{stock.date}
                td #{stock.close_price}
    .pagination 
        - for(var p=1; p<=pagination.lastPage; p++)
            a(href=`/stock/${stock.stock_id}?page=${p}`, class=p==pagination.currentPage?"active":"") #{p}  